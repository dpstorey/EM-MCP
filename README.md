## Enterprise Manager version of John Walley's Tenable OT MCP
Look at mcp to start and stop the docker image - or build out a docker-compose.yaml 
1. place mcp in /usr/local/bin on your EM
2. create /usr/local/mcp/data
3. then `mcp build` will build the docker image and `mcp run` will run it

